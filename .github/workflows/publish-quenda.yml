name: Publish quenda to PyPI

on:
  push:
    tags:
      - 'quenda-v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Extract version from tag
        id: version
        run: |
          # Extract version from tag (e.g., quenda-v0.1.8 -> 0.1.8)
          VERSION=${GITHUB_REF#refs/tags/quenda-v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build package
        run: |
          rm -rf dist/
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: quenda ${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: |
            dist/*.whl
            dist/*.tar.gz
